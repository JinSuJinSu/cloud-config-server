eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
      
  instance:
    prefer-ip-address: true
      
microservice:
   main-service:
      endpoints:
         endpoint:
            uri: http://MAIN-SERVICE/api/test
            
   user-service:
      endpoints:
         endpoint:
            uri: http://USER-SERVICE/api/test
            
   guestbook-service:
      endpoints:
         endpoint:
            uri: http://GUESTBOOK-SERVICE/api/test
            
   board-service:
      endpoints:
         endpoint:
            uri: http://BOARD-SERVICE/api/test
            
   admin-service:
      endpoints:
         endpoint:
            uri: http://ADMIN-SERVICE/api/test
